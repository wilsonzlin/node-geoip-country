{
  "name": "node-geoip-country",
  "version": "0.0.1",
  "main": "dist/client.js",
  "files": [
    "dist/**"
  ],
  "repository": "https://github.com/wilsonzlin/node-geoip-country",
  "author": "Wilson Lin <npm@wilsonl.in>",
  "license": "ISC",
  "scripts": {
    "build-client": "npm run clean && tsc -p ./tsconfig.client.json",
    "build-data": "bash ./src/fetch.sh && ts-node src/build.ts",
    "clean": "rm -rf dist",
    "format": "prettier --write '*.{js,json}' 'src/**/*.ts'",
    "prepublishOnly": "prettier --check '*.{js,json}' 'src/**/*.ts' && npm run build-client && npm run test",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.4",
    "jest": "^27.0.5",
    "prettier": "^2.3.1",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.4"
  }
}
